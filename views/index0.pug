html
head
	style
		include style.css
	title= "improve-page-load-times-with-mp4s"
body
	div(class='container' style="background: linear-gradient(to right, rgba(1,27,55,.8),rgba(0,89,113,.8)), url(https://cdn2.hubspot.net/hubfs/175296/converting-gifs-to-mp4s-1.jpg) no-repeat center / cover;")
		img(class="logo" src="https://cdn2.hubspot.net/hubfs/175296/kyan_images/vieo-logo-2015-WHITE-648977-edited.png")
		div(class='quote')
			p GIFs still have their place. They’re perfect for social media posts, for example. But, if you’re uploading clips to a blog or webpage, MP4 is the way to go to help your page load faster while still looking crisp.
	script.
		const getFontSize = (textLength) => {
			if ( textLength < 259 ) {
			return "45px";
			} else if ( textLength < 450 ) {
				return "35px";
			}
		}

		const boxes = document.querySelectorAll('.quote p')

		boxes.forEach(box => {
		box.style.fontSize = getFontSize(box.textContent.length)
		})